<Window x:Class="ImageSearch.View.ImageImportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ImageSearch.ViewModel"
        xmlns:local="clr-namespace:ImageSearch.View"
        mc:Ignorable="d"
        Title="Image Importer" Height="450" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Content="Image Viewer" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center"/>

        <Button Content="Import Images" Grid.Column="0" Grid.Row="1"
                Command="{Binding ImportImagesCommand}" />
        <ListBox ItemsSource="{Binding FilteredImages}" DisplayMemberPath="Name"
                 SelectedItem="{Binding SelectedImage}"
                  Grid.Row="2" Grid.Column="0" Grid.RowSpan="2" />
        <local:ImageView DataContext="{Binding SelectedImage}" IsEnabled="{Binding DataContext.ImageSelected, RelativeSource={RelativeSource AncestorType=Window} }"
               Grid.Column="1" Grid.Row="2" Grid.RowSpan="2" Grid.ColumnSpan="2" 
                Margin="10" />
        <local:SearchCriteriaView DataContext="{Binding SearchCriteriaDataContext}" IsEnabled="{Binding DataContext.ImagesLoaded, RelativeSource={RelativeSource AncestorType=Window}}"
               Grid.Column="3" Grid.Row="1" Grid.RowSpan="3" />

        <!-- Search Button -->
        <Button Content="Search" Grid.Row="1" Grid.Column="4" IsEnabled="{Binding ImagesLoaded}"
            Command="{Binding SearchCommand}" 
            Margin="0,10,0,0"/>
    </Grid>

</Window>
