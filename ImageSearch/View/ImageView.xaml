<UserControl x:Class="ImageSearch.View.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ImageSearch.ViewModel"
             xmlns:local="clr-namespace:ImageSearch.View"
             mc:Ignorable="d"              >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- File Name -->
        <TextBlock Text="{Binding Name}" Grid.Row="0" Grid.Column="0"/>
        <!-- Size -->
        <TextBlock Text="{Binding Size}" Grid.Row="1" Grid.Column="0"/>
        <!-- Dimensions -->
        <TextBlock Text="{Binding Dimensions}" Grid.Row="2" Grid.Column="0"/>
        <!-- DPI -->
        <TextBlock Text="{Binding Dpi}" Grid.Row="3" Grid.Column="0"/>

        <!-- Tags -->
        <TextBlock Text="Tags:" FontWeight="Bold" Grid.Row="4" Grid.Column="0" VerticalAlignment="Top"/>
        <StackPanel Grid.Row="4" Grid.Column="1">
            <!-- Tag List -->
            <ListBox ItemsSource="{Binding Tags}" SelectionMode="Single" Margin="0,0,0,5">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <!-- Tag -->
                            <TextBlock Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             MinWidth="100" Margin="0,0,5,0"/>

                            <!-- Remove Button -->
                            <Button Content="✕" 
                            Command="{Binding DataContext.RemoveTagCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                            CommandParameter="{Binding .}" 
                            Width="25" Height="25" 
                            Background="Transparent" BorderBrush="Transparent" Foreground="Red"/>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Add New Tag Panel -->
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="NewTagBox" Width="100" Margin="0,0,5,0"/>
                <Button Content="Add"
                Command="{Binding AddTagCommand}"
                CommandParameter="{Binding Text, ElementName=NewTagBox}"/>
            </StackPanel>
        </StackPanel>

        <!-- Metadata Property Pairs -->
        <Expander Header="Metadata" Grid.Row="4" Grid.Column="2" Margin="0,10,0,0">
            <ListBox ItemsSource="{Binding Metadata}" IsSynchronizedWithCurrentItem="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Expander>
    </Grid>
</UserControl>
